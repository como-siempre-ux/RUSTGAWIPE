node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env.*.local

*.tsbuildinfo
next-env.d.ts

.DS_Store
Thumbs.db
.vercel
npm-debug.log*
yarn-error.log*

# Carpeta de originales: material de trabajo, no se publica.
# Anclada a la raíz con la barra inicial: sin ella, en Windows (donde git no
# distingue mayúsculas) esta regla también tapaba `public/imagenes/`, que sí
# tiene que ir al repo porque es de donde Next sirve la portada.
/Imagenes/

# Datos cocinados en el build; se regeneran con `npm run build:data`.
/public/data/
/out/
